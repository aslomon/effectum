---
name: "goal"
description: "Permanent shortcut for effect:goal, the provider-neutral Goal envelope command."
allowed-tools: ["Read", "Write", "Edit", "Bash", "Glob", "Grep"]
effort: "medium"
---

# /goal — Shortcut for effect:goal

`/goal` is a permanent shortcut for `effect:goal`.

Use it to create or update the project-level `GOAL.md`: the provider-neutral work-order envelope that drives PRD, workflow, run, and review decisions.

Follow the full instructions in `effect/goal.md` exactly.

## Next Steps

- → `effect:goal` — Run the canonical Goal command with the same arguments
- → `effect:next` — If you are unsure what to do after the Goal is created
