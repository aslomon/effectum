## Firebase Database

- Firestore for document storage with security rules
- Firebase Admin SDK for server-side operations
- Firebase Auth for authentication (email, social, anonymous)
- Cloud Functions for server-side logic
- Firestore security rules: deny by default, explicit allows
- Structure data for query patterns — denormalize when needed
- Use batched writes and transactions for consistency
- Cloud Storage for file uploads with security rules
