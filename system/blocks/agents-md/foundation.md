# Agent Instructions — {{projectName}}

## Foundation

- Work on **{{projectName}}** using the detected stack: **{{stack}}**
- Read relevant files before making changes — never assume structure
- Make small, focused changes; verify each step before proceeding
- Do not perform destructive operations (delete files, drop tables, truncate data) without explicit confirmation
- Ask before taking external actions (sending emails, posting to APIs, running deploys)
- Prefer reversible operations; use version control checkpoints when making broad changes
- When uncertain about scope or intent, ask a clarifying question rather than guessing
